# 출시 노트 요약 — v1.5.2_34

> 작성일: 2026-08-14
> 비교 기준: app-v1.5.1_34 (2026-08-10)
> 대상 빌드: v1.5.2_34 (준비 중 — release/app-v1.5.2_34 브랜치, bump 커밋 완료)
> 동반 백엔드: 없음 (api 무변경, 라이브 api-v1.3.19 유지)
> 배포 방식: **OTA web-only** — 빌드 번호 34 유지, 스토어 재심사 불필요 (`store-notes:skip`)

## Git 로그 (app-v1.5.1_34..HEAD, --no-merges)

| 해시 | 날짜 | 메시지 |
|------|------|--------|
| 5f67478 | 2026-08-14 | chore: bump version app-v1.5.2_34 |
| 7b36fa6 | 2026-08-14 | fix(home): 손익 카드 값 겹침 해소 + 배분 도넛 빈 상태 원인별 안내 |
| 7ceed99 | 2026-08-10 | docs(backlog): 푸시 iOS 실기기 검증 완료 + 백버튼 항목 정정 |
| 14134a4 | 2026-08-10 | docs(backlog): 일별 시장일지 요구사항 감사 결과 추가 |
| 0e87f40 | 2026-08-10 | docs(release): v1.5.1_34 OTA 발행 결과 + OTA_REQUIRED_NATIVE 1.5.0 인상 기록 |

## 동기간 spec-history 항목

- 없음 (직전 태그 이후 `docs/spec-history/` 신규 항목 없음. 최신 항목은 `2026-07-29-push-firebase-unify.md`)

## 분류표

| 라벨 | 항목 | 출시 노트 반영 |
|------|------|--------------|
| FIX | 홈 손익 카드(평가손익·확정손익·이달 확정)에서 금액이 옆 카드 위로 넘쳐 겹쳐 보이던 문제 수정 — 카드 안에서 잘라내고 손익 금액을 항상 축약 표기(예: 188만원), 값 폰트 13px (`7b36fa6`) | ✓ (다음 네이티브 제출 노트에 누적) |
| FIX | 홈 자산 배분(종목별·계좌별)이 항상 "데이터 없음"으로만 표시되던 문제 — 보유 없음 / 시세 조회 실패 / 환율 미상 / 조회 중을 구분해 안내. 특히 시세 조회 실패 시 보유 종목 목록에는 종목이 보이는데 도넛만 비던 모순, 해외 보유 + 환율 미상에서 "조회 중"이 끝나지 않던 상태가 해소됨 (`7b36fa6`) | ✓ (다음 네이티브 제출 노트에 누적) |
| INTERNAL | 홈 컴포넌트 테스트 2종 추가 — 손익 축약 표기 / 배분 빈 상태 4분기 (`7b36fa6`) | ✗ |
| INTERNAL | 백로그·직전 릴리즈 노트 문서 갱신 (`7ceed99`, `14134a4`, `0e87f40`) | ✗ |
| INTERNAL | 버전 bump (`5f67478`) | ✗ |

> 이번 릴리즈도 OTA web-only 라 스토어 노트를 만들지 않는다. 위 FIX 2건은 **다음 네이티브 제출 때** `release-notes` 를 `since app-v1.5.0_34`(= 마지막 네이티브 태그) 로 돌려 v1.5.1 의 2건과 함께 누적 스토어 노트로 묶는다.

## 검증 결과

- app-store-ko.md: 해당 없음 (store-notes:skip)
- play-store-ko.md: 해당 없음 (store-notes:skip)
- 버전 3곳 일치: `make version-check` → `[app] in sync: 1.5.2 build 34` ✓
- 빌드 번호 유지 확인: 직전 34 → 이번 34 (OTA web-only 규칙대로 `bump-build` 생략) ✓
- web-only 판정 교차검증: `app/package.json`·`capacitor.config.ts`·`ios/`·`android/`(버전 파일 제외) diff 없음 — 네이티브 플러그인 추가로 인한 오판 아님 ✓
- 테스트: app `vitest run` 321 passed (41 files) / `tsc --noEmit` 클린 ✓ (api 무변경이라 pytest 생략)

## 배포 체크리스트 (출시 노트 외 운영 작업)

- **DB 마이그레이션: 불필요** — `api/alembic/` 변경 없음 (repo head `0018_drop_import_staging` 유지)
- **BE 배포: 불필요** — `api/` diff 0건. main push 로 Coolify 가 트리거되더라도 백엔드 코드 변경분이 없음
- **MIN_SUPPORTED_VERSION: 변경 불필요** — 커밋된 값 `1.3.0` 유지. API 호출·응답 shape 변경이 전혀 없는 FE 표시 전용 수정 (⚠️ 운영 실제 값의 SSOT 는 Coolify env — git `.env.production` 과 다를 수 있음)
- **모바일 스토어 제출: 불필요** — OTA 번들 배포로 반영, 빌드 번호 34 유지
- **실행 순서**: main push → OTA 번들 발행 (선행 작업 없음)

## 다음 빌드를 위한 메모

- **스토어 노트 누적**: 다음 네이티브 제출 시 `release-notes ... since app-v1.5.0_34` 로 호출해 v1.5.1(알림 중복 제거·상태 칩 숨김) + v1.5.2(홈 손익 카드 겹침·자산 배분 안내) 를 함께 묶을 것
- **`OTA_REQUIRED_NATIVE` 는 1.5.0 유지** — 스토어 라이브 바이너리가 그대로이므로 이번 발행에서 인상 없음. 1.4.3_33 이하 기기는 계속 OTA 제외
- **번들 버전 재사용 금지** — 이번 발행은 신규 마케팅 버전 `1.5.2` 로 나가야 한다. 기존 버전 이름으로 재발행하면 capgo 가 같은 버전으로 식별해 기존 설치에 전달되지 않는다
- **미검증 사항**: 손익 카드 수정은 jsdom 테스트(문자열·분기)로만 확인했고 실제 렌더 폭은 측정하지 않았다. 번들 발행 후 실기기에서 최소폭(320px 급) 기기 또는 큰 금액 계정으로 홈 카드를 한 번 눈으로 확인할 것
- 분석 탭 `SummaryCards`(2열 카드)는 이번 수정 범위에서 제외 — 폭 여유가 있고 신고 대상도 아님. 동일 증상이 보고되면 같은 방식(truncate + 축약)으로 처리
