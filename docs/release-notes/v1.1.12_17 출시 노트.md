짧은 버전 (Play Store/App Store 한 화면용)

  - 토스증권 PDF 업로드 시 단가·수수료가 어긋나던 문제를 수정했습니다.
  - 거래 일괄 등록에서 매도 보유부족 행만 자동 제외하고 나머지는 그대로 등록되도록 개선했습니다.
  - 거래 등록·수정 중 키보드가 올라올 때 뒤 화면이 비치던 현상을 수정했습니다.
  - 당겨서 새로고침 인디케이터가 더 잘 보이도록 다듬고 햅틱 피드백을 추가했습니다.
  - 계좌·종목 선택 드롭다운의 너비와 간격을 정돈했습니다.

  조금 더 풀어쓴 버전 (필요 시)

  [개선]
  - 토스증권 거래내역서 업로드: 최신 PDF 양식에 추가된 ‘정산금액’ 컬럼을 인식하도록
    파서를 업데이트했습니다. 이전에는 단가·수수료 값이 한 칸씩 밀려 잘못 등록되는
    경우가 있었습니다.
  - 거래 일괄 등록 미리보기: 매도 보유부족이 발생한 종목 그룹만 자동 제외하고,
    나머지 정상 거래는 그대로 등록됩니다. 제외된 건수는 결과 화면에서 확인할 수 있습니다.
  - 거래 등록/수정 패널: 키보드가 표시될 때 뒤의 거래 목록이 비쳐 보이던 현상을
    수정했습니다.
  - 당겨서 새로고침: 로딩 아이콘이 하단에서 잘리지 않도록 수정하고, 새로고침 시
    진동 피드백을 추가했습니다.
  - 계좌·종목 선택 드롭다운의 너비와 간격을 정렬했습니다.
  - 기록 탭의 업로드 버튼 라벨을 “거래내역서 업로드”로 명확히 했습니다.

  [내부]
  - UI 컴포넌트 기반을 정리하여 향후 화면 안정성과 일관성을 강화했습니다.

  사내/체인지로그용 (운영 메모)

  v1.1.12 (BE: dual prefix 호환 / FE: versionName 1.1.12)
  - BE: 라우터 prefix `/api/*` + `/*` 동시 지원 (선배포 필수). 옛 앱 호환 유지.
  - FE: api-client ROUTES 신 경로(`/accounts` 등) 전환.
  - FE: shadcn primitive base-ui → radix-ui 전환 (의존성 대규모 변경, lockfile 갱신).
  - iOS 키보드 패널 투과 버그 수정.
  - 토스 PDF 정산금액 컬럼 파싱 수정.
  - 매도 보유부족 그룹만 import 제외 로직 개선.
  - pull-to-refresh 가시성 + 햅틱.
  - 드롭다운 너비/간격 정합성.

  배포 순서: BE → FE 스토어 제출. legacy `/api/*` alias 는 당분간 유지 (옛 앱 사용자 흡수).