{
  "name": "invest-note",
  "private": true,
  "scripts": {
    "dev": "pnpm -C app dev",
    "build": "pnpm -C app build",
    "lint": "pnpm -C app lint",
    "test": "pnpm -C app test",
    "tsc": "pnpm -C app exec tsc --noEmit"
  }
}
